apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: visa-app
type: Opaque
data:
  # Base64 encoded values - REPLACE WITH YOUR ACTUAL VALUES
  # Use: echo -n "your_password" | base64
  postgres-password: "REPLACE_WITH_BASE64_ENCODED_PASSWORD"
  redis-password: "REPLACE_WITH_BASE64_ENCODED_PASSWORD" 
  google-api-key: "REPLACE_WITH_BASE64_ENCODED_API_KEY"
  openai-api-key: "REPLACE_WITH_BASE64_ENCODED_API_KEY"
  anthropic-api-key: "REPLACE_WITH_BASE64_ENCODED_API_KEY"

# Instructions:
# 1. Copy this file to app-secrets.yaml
# 2. Replace placeholder values with base64-encoded secrets
# 3. Apply: kubectl apply -f app-secrets.yaml
# 4. NEVER commit app-secrets.yaml to git!